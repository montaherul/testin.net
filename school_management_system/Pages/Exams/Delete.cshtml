@page "{id:int}"
@model school_management_system.Pages.Exams.DeleteModel

<h2>Delete Exam</h2>
<div>
    <h4>Are you sure you want to delete this exam?</h4>
    <dl class="row">
        <dt class="col-sm-2">Name</dt>
        <dd class="col-sm-10">@Model.Exam.ExamName</dd>
        <dt class="col-sm-2">Class</dt>
        <dd class="col-sm-10">@Model.Exam.Class?.ClassName</dd>
    </dl>
    <form method="post">
        <button type="submit" class="btn btn-danger">Delete</button>
        <a asp-page="Index" class="btn btn-secondary">Cancel</a>
    </form>
</div>